# -*- coding: utf-8 -*-
"""
Compiled extensions: ``mdutils`` (Fortran) and ``gaussian_density``
(Fortran), plus ``mdutils_cpp_omp`` (C++).  Import the submodule you
need directly, e.g. ``from samos.lib.mdutils import get_com_positions``
-- that way importing ``samos.lib`` itself never requires a compiler
that happens to be missing for a routine you are not using.
"""
